import { Placeholder } from './Placeholder';

// Buyer surfaces
export const DiscoverScreen = () => (
  <Placeholder title="Discover" note="University & ambassador search, filters, featured guides." />
);
export const BookingsScreen = () => (
  <Placeholder title="Bookings" note="Upcoming & past bookings, receipts, cancel & review." />
);
export const MessagesScreen = () => (
  <Placeholder title="Messages" note="1:1 chat with ambassadors (Socket.IO)." />
);
export const ProfileScreen = () => (
  <Placeholder title="Profile" note="Account, receipts, settings, role switch, logout." />
);

// Ambassador surfaces
export const RequestsScreen = () => (
  <Placeholder title="Requests" note="Incoming tour requests — accept/decline in the window." />
);
export const ListingsScreen = () => (
  <Placeholder title="Listings" note="Create & manage listings and pricing." />
);
export const EarningsScreen = () => (
  <Placeholder title="Earnings" note="Gross / commission / net, balance, payout history." />
);

// Auth
export const LoginScreen = () => (
  <Placeholder title="Sign in" note="JWT auth; tokens stored in SecureStore." />
);
