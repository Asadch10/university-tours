# Expo / Metro
.expo/
dist/
web-build/

# Native projects — regenerated by `expo prebuild` / `expo run:*` (CNG).
# Committing these (Pods, build/) bloats the repo; they're build output, not source.
/ios/
/android/

# Local env
.env*.local

# macOS
.DS_Store
